/* Creating a constant variable called LEVELS and assigning it to an object with three properties. */
const LEVELS = {
    NORMAL: 'normal',
    URGENT: 'urgent',
    BLOCKING: 'blocking',
  };
  
  export default LEVELS;